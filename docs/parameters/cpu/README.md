# CPU / Boot Parameters

Parameters that require kernel boot arguments (via MachineConfig or PerformanceProfile). Cannot be set via TuneD sysctl.

| Parameter | Status | Applies to |
|-----------|--------|-----------|
| [cstates](cstates/) | Under investigation | BM workers |
