# [vm] Parameters

Virtual memory settings applied via TuneD's `[vm]` section.

```ini
[vm]
transparent_hugepages=never
```

| Parameter | Status | Applies to |
|-----------|--------|-----------|
| [transparent_hugepages](transparent_hugepages/) | Ready | Workers=never, CP/Infra=always (kept) |
